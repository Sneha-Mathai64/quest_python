numbers = [23, 7, 19, 41, 29, 3, 47]
print('Max number = ', max(numbers))#Finds the maximum value in the list numbers
print('Min number = ', min(numbers))#Finds the minimum value in the list numbers
print('Number of elements = ', len(numbers))#returns the length of elements
print('Sorted numbers = ', sorted(numbers))# Returns a new list with the elements of numbers sorted in ascending order.
print('Numbers = ', numbers)
numbers.sort() # This will sort the original list in place
print('Numbers = ', numbers)
